#!/usr/bin/node

/* Object.values */
// Returns an array of a given object's own enumerable property values.

let car = { brand: 'Toyota', model: 'Camry' };
console.log(Object.values(car)); // Outputs: ['Toyota', 'Camry']

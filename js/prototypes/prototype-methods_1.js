#!/usr/bin/node

/* Object.keys */
// Returns an array of a given object's own enumerable property names.

let car = { brand: 'Toyota', model: 'Camry' };
console.log(Object.keys(car)); // Outputs: ['brand', 'model']

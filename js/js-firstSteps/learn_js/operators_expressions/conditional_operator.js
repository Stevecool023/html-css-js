/*
 * Conditional (Ternary) Operator
 * The ternary operator provides a concise way to write simple conditional statements.
 */

let age = 20;
let canVote = (age >= 18) ? "Yes" : "No";

console.log('canVote:', canVote); // Output: Yes

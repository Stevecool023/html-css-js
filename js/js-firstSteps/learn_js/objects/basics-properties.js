/*
 * Objects are used to store data in key-value pairs.
 * Object basics and properties
 */

let person = {
	name: "John",
	age: 25,
	isStudent: false
};

console.log(person.name); // Accessing property: John
console.log(person.age); // Accessing property: 25
console.log(person.isStudent);

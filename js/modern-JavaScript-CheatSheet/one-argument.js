const double = (x) => x * 2; // this arrow function only takes one parameter
const tripple = x => x * 3; // this arrow function only takes one parameter.
console.log(double(4));
console.log(tripple(4));

function myFunction() {
	var myVar = "Nick"
	console.log(myVar); // "Nick" - myVar is accessible inside the function
}
myFunction();
/* console.log(myVar); // Throws a ReferenceError, myVar is not accessible outside function. */

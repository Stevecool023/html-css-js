/*
 * Template literals is an expression interpolation for single and multiple-line strings.
 */
const name = "Nick";
const templateLiteral = `Hello ${name}, the following expression is equal to four : ${2+2}`;
console.log(templateLiteral);

/*
 * When assigning a variable to an object property,
 * if the variable name is equal to the propery name,
 * one can do the following:
 */
const x = 10;
const myObj = { x };
console.log(myObj.x) // 10
